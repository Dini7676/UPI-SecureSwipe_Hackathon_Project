# Frontend (React + Tailwind)

- OTP login (mobile/email)
- User, Merchant, Admin dashboards
- QR generator, transaction history
- Axios API service

## Dev

```bash
npm install
npm run dev
```

Set `VITE_API_BASE` in `.env` to backend URL.
